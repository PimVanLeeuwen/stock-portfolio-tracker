# ── Signal ────────────────────────────────────────────────────
# Port the signal-api container exposes on the host
SIGNAL_PORT=8080

# ── stock-bot ─────────────────────────────────────────────────
# Set to "true" to run once and exit (useful for testing)
RUN_ONCE=false
LOG_LEVEL=INFO

# ── Data providers (optional – yfinance works without keys) ──
FINNHUB_API_KEY=
ALPHAVANTAGE_API_KEY=

