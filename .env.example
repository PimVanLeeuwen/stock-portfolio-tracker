# ── Telegram ──────────────────────────────────────────────────
# Create a bot via @BotFather on Telegram, paste the token here.
TELEGRAM_BOT_TOKEN=
# Your chat ID (or group chat ID). To find it:
#   1. Send any message to your bot
#   2. Visit https://api.telegram.org/bot<TOKEN>/getUpdates
#   3. Look for "chat":{"id": 123456789 }
TELEGRAM_CHAT_ID=

# ── stock-bot ─────────────────────────────────────────────────
# Set to "true" to run once and exit (useful for testing)
RUN_ONCE=false
LOG_LEVEL=INFO

# ── Schedule ──────────────────────────────────────────────────
# Standard cron: minute hour day-of-month month day-of-week
# Examples:
#   "10 8,17 * * 1-5"   → 08:10 & 17:10 on weekdays
#   "0 9 * * *"          → 09:00 every day
#   "*/30 8-18 * * 1-5"  → every 30 min during market hours on weekdays
CRON_SCHEDULE=10 8,17 * * 1-5
TZ=Europe/Amsterdam

# ── Portfolio ─────────────────────────────────────────────────
# Format: SYMBOL:UNITS:COST_BASIS (cost_basis optional)
# Comma-separated. Overrides config.yml positions if set.
POSITIONS=AAPL:12:148.20,MSFT:8:310.50,ASML.AS:5,GOOGL:15:131.40
INDICES=^GSPC,^NDX

# ── Data providers (optional – yfinance works without keys) ──
FINNHUB_API_KEY=
ALPHAVANTAGE_API_KEY=
